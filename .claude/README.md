# Shared agent context

This directory contains versioned context shared by Claude, Codex, and future automation. Local permissions and machine-specific settings remain ignored.

Read `AGENTS.md` first, then use the files under `context/` as a map of the current system. Update them in the same change when architecture or operating instructions change.
